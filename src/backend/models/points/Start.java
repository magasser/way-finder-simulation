package backend.models.points;

import javafx.scene.paint.Color;

public class Start extends Point {
    public Start(final long x,final long y) {
        super(x, y);
        color = Color.BLUE;
    }
}
