package backend.models.entities;

import javafx.scene.paint.Color;

public class Ball extends Entity {
    public Ball(final long x, final long y, final Color color, final int steps) {
        super(x, y, color, steps);
    }
}
